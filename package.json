{
  "name": "@minukood/typescript-boilerplate",
  "version": "0.0.5",
  "description": "typescript-boilerplate",
  "repository": "git@github.com:JakobPetersson/typescript-boilerplate.git",
  "author": "Jakob Petersson <github@jakobpetersson.se>",
  "license": "MIT",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test:lint": "tslint --fix -p ./tsconfig.test.json",
    "test:unit": "jest",
    "test": "run-s test:*",
    "build": "rm -rf dist/ && tsc",
    "precommit": "run-s test build && git add -A dist",
    "preversion": "run-s precommit",
    "postversion": "git push && git push --tags"
  },
  "devDependencies": {
    "@types/jest": "23.3.9",
    "husky": "1.1.3",
    "jest": "23.6.0",
    "npm-run-all": "4.1.3",
    "ts-jest": "23.10.4",
    "tslint": "5.11.0",
    "typescript": "3.1.6"
  },
  "jest": {
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "testRegex": "(/__tests__/.*|(\\.|/)(test|spec))\\.[tj]sx?$",
    "roots": [
      "<rootDir>/src/",
      "<rootDir>/test/"
    ],
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js",
      "jsx",
      "json",
      "node"
    ]
  }
}
